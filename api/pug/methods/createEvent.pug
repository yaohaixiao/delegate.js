section#method-createEvent.section
  header.section__header
    h3.section__h3 createEvent(type[, detail = null, bubbles = true, cancelable = true])
  div.section__content
    h4.section__h4 Description
    p 创建自定义事件（CustomerEvent）。
    dl.section__dl
      dt.section__dt Since：
      dd.section__dd 1.8.0
    h4.section__h4 Parameters
    h5.section__h4 type
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd String
    p （必须）事件类型（名称）。
    h5.section__h4 detail
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Object
    dl.section__dl
      dt.section__dt Default：
      dd.section__dd null
    p （可选）传递给自定义事件的数据，默认为 null。
    h5.section__h4 bubbles
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Boolean
    dl.section__dl
      dt.section__dt Default：
      dd.section__dd true
    p （可选）是否支持冒泡，默认为 true。
    h5.section__h4 cancelable
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Boolean
    dl.section__dl
      dt.section__dt Default：
      dd.section__dd true
    p （可选）是否可以取消，默认为 true。
    h4.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd CustomerEvent。
    p CustomerEvent 实例。
    pre.section__pre
      code.section__code.
        &lt;div id="nav" class="nav"&gt;
          &lt;a id="service" class="anchor" href="https://www.yaohaixiao.com/serivce"&gt;Service&lt;/a&gt;
          &lt;a id="help" class="anchor" href="https://www.yaohaixiao.com/help"&gt;Help&lt;/a&gt;
        &lt;/div&gt;

        const $nav = document.querySelector('#nav')
        const $service = document.querySelector('#serivce')
        const $emitter = delegate($nav)
        const logEvent = $emitter.createEvent('log', {
          name: 'Yao',
          hi() {
            console.log('hi！！！')
          }
        })

        const logHandler = function(evt) {
          console.log('detail', evt.detail)
          console.log('type', evt.type)
        }

        // 或者
        $service.dispatchEvent(logEvent)
