/**
 * 检测测试数据是否为 null
 * ========================================================================
 * @param val
 * @returns {boolean}
 */
const isNull = (val) => {
  return val === null
}

export default isNull
