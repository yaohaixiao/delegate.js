section#method-destroy.section
  header.section__header
    h3.section__h3 destroy()
  div.section__content
    h4.section__h4 Description
    p 解除 DOM 元素（$el）全部包括子元素的所有事件处理器。
    h4.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Emitter。
    p 返回 Emitter 对象（实例）。
    pre.section__pre
      code.section__code.
        const handler = function(evt) {
          const $li = evt.delegateTarget
          const $textarea = document.querySelector('#log-textarea')

          $textarea.value += `你点击的 li 节点的 id 为 ${$li.id}\r`
        }

        const $emitter = delegate('#list')

        // 解除所有绑定事件
        $emitter.destroy()
        // => 点击 li 元素将不会执行 handler 事件处理器
