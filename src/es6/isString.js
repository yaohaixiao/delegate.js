/**
 * 判断是否为 String 类型值
 * ========================================================================
 * @param {String} val - 待检测的字符串
 * @returns {Boolean}
 */
const isString = (val) => {
  return typeof val === 'string'
}

export default isString
