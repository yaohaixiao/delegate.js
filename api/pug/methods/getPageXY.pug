section#method-getPageXY.section
  header.section__header
    h3.section__h3 getPageXY(evt)
  div.section__content
    h4.section__h4 Description
    p 获取事件触发时的 pageX 和 pageY 数组数据。
    h4.section__h4 Parameters
    h5.section__h4 evt
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Event
    p （必须）事件对象。
    h4.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Array。
    p 返回事件触发时的数组数据：[pageX, pageY]。
    pre.section__pre
      code.section__code.
        const $emitter = delegate('#list')
        const showLog = function (evt) {
          const pageXY = $emitter.getPageXY(evt)

          console.log(`pageX 为：${pageXY[0]}`)
          console.log(`pageY 为：${pageXY[1]}`)
        }

        $emitter.on('.item', 'click', showLog)
